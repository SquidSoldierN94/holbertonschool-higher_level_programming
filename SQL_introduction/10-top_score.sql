-- This script selects the score and name from 'second_table' and orders the results by score in descending order.
-- Save this file as 10-select_order_by_score_desc.sql

-- The SELECT command retrieves specified columns from the specified table and orders the results.
SELECT score, name FROM second_table ORDER BY score DESC;
