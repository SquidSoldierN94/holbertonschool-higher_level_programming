-- This script creates the database 'hbtn_0c_0' if it does not already exist.
-- Save this file as 1-create_database.sql

-- The CREATE DATABASE IF NOT EXISTS command creates a new database only if it does not already exist.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
